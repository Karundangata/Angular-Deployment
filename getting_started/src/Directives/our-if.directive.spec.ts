import { OurIfDirective } from './our-if.directive';

describe('OurIfDirective', () => {
  it('should create an instance', () => {
    const directive = new OurIfDirective();
    expect(directive).toBeTruthy();
  });
});
